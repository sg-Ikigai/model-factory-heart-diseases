# ─────────────────────────────────────────────────────────────
#  Heart Disease Experiment - Python 3.12
#  Install core packages:
#    pip install -r requirements.txt
#
#  PyTorch MUST be installed with CUDA support separately:
#    pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu128
#  (cu128 = CUDA 12.8; supports Blackwell RTX PRO 1000 and all prior arches)
#  Driver ≥ 525.60 required for CUDA 12.x; driver 596.58 supports CUDA 13.2.
# ─────────────────────────────────────────────────────────────

# Core ML (install torch separately as documented above)
# Versions are pinned to the ones used for the reported results.
scikit-learn==1.9.0
numpy==2.5.1
pandas==3.0.5

# Visualisation
plotly==6.9.0

# Kaggle data download
kaggle==2.2.4

# Notebook runtime
ipykernel==7.3.0
ipywidgets==8.1.8
